class AI {
  repeat() {}

  sense() {
    // Sense: The agent detects - or is told about - things in their environment that may
    // influence their behaviour (e.g. threats nearby, items to collect, points of interest to investigate)
  }

  think() {
    // Think: The agent makes a decision about what to do in response (e.g. considers
    // whether it is safe enough to collect items, or whether it should focus on fighting or hiding first)
  }

  act() {
    // Act: The agent performs actions to put the previous decision into motion (e.g. starts moving
    // along a path towards the enemy, or towards the item, etc)
  }

  decisionTree() {}
}
